# GitHub Actions Fix History Log

This file tracks the fixes suggested for GitHub Actions build errors and their implementation status.

| 1     | Revert `CardThemeData` to `CardTheme` and `withValues` to `withOpacity` | Yes | Yes | Resolved |
| 2     | Upgrade `ndkVersion` to `26.1.10909125` in `build.gradle.kts` | Yes | Yes | Applied (Awaiting Permission) |

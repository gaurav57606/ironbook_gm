--- lib/core/providers/member_provider.dart
+++ lib/core/providers/member_provider.dart
@@ -125,6 +125,8 @@
   final now = ref.watch(clockProvider).now;
   final sort = ref.watch(memberSortProvider);

+  // Optimization: Evaluates search and tab filters in a single pass to eliminate
+  // redundant O(N) list iterations and intermediate list memory allocations.
   List<MemberSnapshot> filtered = [];

   for (final m in members) {
